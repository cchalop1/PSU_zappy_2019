name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: epitechcontent/epitest-docker
    steps:
    - uses: actions/checkout@v2.2.0
    - name: make
      run: make re
